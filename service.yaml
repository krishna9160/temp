
apiVersion: v1
kind: Service
metadata:
  name: myapp-lb
spec:
  type: LoadBalancer   # ✅ important
  selector:
    app: myapp
  ports:
    - port: 80          # exposed port
      targetPort: 80    # container port
